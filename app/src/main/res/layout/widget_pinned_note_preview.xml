<?xml version="1.0" encoding="utf-8"?>
<!-- C-group task 9 — picker preview for the pinned-note widget. See widget_quick_actions_preview.xml
     for why previews are their own layouts rather than the real one scaled down.

     The real layout shows a title plus several body lines at reading sizes. In the picker's cell
     those lines collapse into the dark slab with white bars that the bug report shows. This draws
     the same idea at preview scale: one title line and two short rules standing in for body text,
     which is what a preview is for — recognising the widget, not reading it. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="8dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_label_pinned"
        android:textColor="@color/widget_text"
        android:textSize="10sp"
        android:maxLines="1" />

    <View
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_marginTop="6dp"
        android:background="@drawable/widget_hint_pill" />

    <View
        android:layout_width="64dp"
        android:layout_height="3dp"
        android:layout_marginTop="4dp"
        android:background="@drawable/widget_hint_pill" />
</LinearLayout>

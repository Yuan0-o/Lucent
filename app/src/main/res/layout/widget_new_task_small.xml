<?xml version="1.0" encoding="utf-8"?>
<!-- 1x1: just the glyph on its chip — legible at a glance, never the app icon. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_card_gradient">
    <FrameLayout
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="center"
        android:background="@drawable/widget_glyph_chip">
        <ImageView
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_widget_task"
            android:contentDescription="@string/widget_label_new_task" />
    </FrameLayout>
</FrameLayout>

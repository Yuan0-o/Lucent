<?xml version="1.0" encoding="utf-8"?>
<!-- "Pinned note" widget: shows the most recently updated pinned note's title and a short preview.
     Tapping opens that note (or, when there's no pinned note, a new-note composer). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_pinned_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_pinned_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/widget_pinned_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="6dp"
        android:textColor="#CCFFFFFF"
        android:textSize="13sp"
        android:maxLines="6"
        android:ellipsize="end" />
</LinearLayout>

<?xml version="1.0" encoding="utf-8"?>
<!-- 3x1 and up: adds the checklist glyph and the next-due line under the bar. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:background="@drawable/widget_bg"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">
        <TextView
            android:id="@+id/progress_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/widget_label_progress"
            android:textColor="@color/widget_text_muted"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end" />
        <ImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@drawable/ic_widget_progress"
            android:alpha="0.7"
            android:contentDescription="@string/widget_label_progress" />
    </LinearLayout>
    <TextView
        android:id="@+id/progress_fraction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="—"
        android:textColor="@color/widget_text"
        android:textSize="29sp"
        android:textStyle="bold"
        android:maxLines="1" />
    <ProgressBar
        android:id="@+id/progress_bar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="9dp"
        android:layout_marginTop="8dp"
        android:progressDrawable="@drawable/widget_progress_track"
        android:max="100"
        android:progress="0" />
    <TextView
        android:id="@+id/progress_next"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="7dp"
        android:text=""
        android:textColor="@color/widget_text_muted"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
